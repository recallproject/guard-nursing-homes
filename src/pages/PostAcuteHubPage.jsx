import SettingHomePage from './SettingHomePage';

/** Home health / inpatient rehab / long-term acute care setting homes. */
export default function PostAcuteHubPage({ settingId }) {
  return <SettingHomePage settingId={settingId} />;
}
